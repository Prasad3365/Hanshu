open a folder ----> go to path -----> cmd ----> code.
extensions ------> Material icon Theme by Pkief.

HTML==== Hyper text markup language started in 1992 by CREN 
GML(generalized ML) => SGML (standard GML)=HTML.
current version of HTML is HTML5.
HTML3---> P, Heading,a,img.
HTML4----> tables,lists,forms,media.
HTML5---->Advanced---->semantic,Dragn Drop,canvas,geolocation.

HTML is used for the content or structure of the web page it includes 
links, tables, data,forms,images,media etc...

Every thing in HTML is done by Tags.
<tagname>  -----> open tag
</tagname> -----> close tag.

Whole HTML is in HTML element. Inside HTML element you will have head and body elements.

Tag vs element
<h1> ---> h1 open tag content
</ha> ---> h1 close tag 

Element contains opentag + content + closetag.

In VS Code there are some shortcuts known as emmets.....
